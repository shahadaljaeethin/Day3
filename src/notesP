primitive vs non primitive.

primitive bedaeeyh like int double etc
non pri from devs
arrays are refrence not premitive?

refrence can print null but primituve will show error
arrayLisy. add with index does shift not overwrite it.

if(list.remove) -> will return true if works well, or false of error.

we can use for each only when we assign the list to an object f.e: Al<String>
-------
argument = parameters
        ArrayList<String> names = new ArrayList<>();
